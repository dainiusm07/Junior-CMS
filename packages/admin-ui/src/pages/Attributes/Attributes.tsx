import { Typography } from '@material-ui/core';
import React from 'react';

const Attributes: React.FC = () => {
  return (
    <div>
      <Typography color="textPrimary">Attributes</Typography>
    </div>
  );
};

export default Attributes;
