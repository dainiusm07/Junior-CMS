{
  "name": "junior-cms",
  "version": "0.0.1",
  "private": true,
  "author": "Dainius Masevicius",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "lerna clean --yes && rm -rf node_modules",
    "fresh": "yarn clean && yarn",
    "codegen": "ts-node scripts/generate-graphql-types.ts",
    "start:dev": "lerna run start:dev --stream",
    "watch": "lerna run watch --stream"
  },
  "dependencies": {
    "lerna": "^3.22.1"
  },
  "devDependencies": {
    "@graphql-codegen/cli": "^1.20.1",
    "@graphql-codegen/typescript": "^1.21.0",
    "graphql": "^15.5.0",
    "ts-node": "^9.1.1",
    "typescript": "^4.1.5"
  }
}
